# Benchmark fixtures

The driver creates minimal deterministic account mappings in
`workloads/setup.sql`. Large 100k/1M/10M row fixtures are a v0.2 deliverable
and should be generated outside timed sections.
